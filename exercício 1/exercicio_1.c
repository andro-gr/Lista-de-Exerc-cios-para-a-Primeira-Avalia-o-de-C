#include <stdio.h>

int main() {
    // Usando um loop for para iterar de 1 a 100
    for (int i = 1; i <= 100; i++) {
        // Verificando se o número atual é par
        if (i % 2 == 0) {
            // Imprimindo o número par
            printf("%d ", i);
        }
    }
    printf("\n"); // Nova linha após imprimir tod

